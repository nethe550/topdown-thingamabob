/**
 * @typedef {{ x: number, y: number }} Vector2
 */

/**
 * @param {number} x
 * @param {number} y
 * @returns {Vector2}
 */
const Vector2 = (x, y) => { return { x: x , y: y } }

export default Vector2;